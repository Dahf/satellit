# Kopieren nach .env — wird von docker compose gelesen.
PUSHOVER_TOKEN=          # App-Token aus pushover.net
PUSHOVER_USER=           # User-Key aus pushover.net
STOOQ_APIKEY=            # optional, Fallback-Kursquelle (Key per Captcha auf stooq.com)
SATELLIT_TZ=Europe/Berlin
